include-file = ~/.config/polybar/colors.ini
include-file = ~/.config/polybar/modules.ini
include-file = ~/.config/polybar/applications.ini

[global/wm]
; Adjust the _NET_WM_STRUT_PARTIAL top value
;   Used for top aligned bars
; Supports any percentage with offset relative to the monitor height
margin-bottom = 0

; Adjust the _NET_WM_STRUT_PARTIAL bottom value
;   Used for bottom aligned bars
; Supports any percentage with offset relative to the monitor height
margin-top = 0

[bar/top]

include-file = ~/.config/polybar/barglobals.ini

; Put the bar at the bottom of the screen
bottom = false

; Modules are added to one of the available blocks
;   modules-left = cpu ram
;   modules-center = xwindow xbacklight
;   modules-right = ipc clock
modules-left = alsa backlight
modules-center = battery network 
modules-right = 

[bar/bottom]

include-file = ~/.config/polybar/barglobals.ini

; Put the bar at the bottom of the screen
bottom = true

; Modules are added to one of the available blocks
;   modules-left = cpu ram
;   modules-center = xwindow xbacklight
;   modules-right = ipc clock
modules-left = 
modules-center = xworkspaces
modules-right = 
